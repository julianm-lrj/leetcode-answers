const sqrt= (x: number): number => {
    /*
    let a = x
    let c = 0.00001
  
    while (true) {  
        let b = (a + x / a) / 2;  
        if (Math.abs(b - a) < c) {  
            break
        }  
        a = b
    }  
  
    return Math.floor(a)
    */
   return Math.floor(Math.sqrt(x))
}

